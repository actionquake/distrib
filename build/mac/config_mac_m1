# for compilation on M1 mac with clang and Xcode, use
# make clean;make;strip -x q2pro;strip -x q2proded
# make strip does not wirk with clang
CONFIG_PNG=y
CONFIG_JPEG=y
CONFIG_HTTP=y
CONFIG_ANTICHEAT_SERVER=y
CONFIG_MVD_SERVER=y
CONFIG_MVD_CLIENT=y
CONFIG_VARIABLE_SERVER_FPS=y
CONFIG_CLIENT_GTV=y
CONFIG_PACKETDUP=y
CONFIG_TESTS=y
CONFIG_DEBUG=y
#CFLAGS=-Werror   # OpenAL-related warnings can be ignored
CONFIG_GAME_BASE=action


CONFIG_OPENAL=y
#had problems compiling with OpenAL on clang, disabled for now
#CONFIG_FIXED_LIBAL=y
#AL_CFLAGS = -framework OpenAL
#AL_LIBS = -framework OpenAL

CONFIG_PATH_DATA=.
CONFIG_PATH_LIB=.
CONFIG_PATH_HOME=~/Applications/AQ.app/Contents/MacOS
